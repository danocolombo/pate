import React from 'react';

const RegisteredDetails = ({ user, key }) => {
    return (
        <>
            <h3>{user.login}</h3>
            <br />
        </>
    );
};

export default RegisteredDetails;
